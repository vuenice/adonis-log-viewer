import { defineConfig } from 'vite'

/**
 * Required by @adonisjs/assembler for `ace serve` / `ace build`.
 * This example does not ship a first-party frontend; the log viewer UI comes from adonis-log-viewer.
 */
export default defineConfig({})
